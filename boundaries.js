import Boundary from './Boundary.js';

const boundaries = [
    new Boundary({ position: { x: 40,  y: 40 } }),
    new Boundary({ position: { x: 80,  y: 40 } }),
    new Boundary({ position: { x: 120, y: 40 } }),
    new Boundary({ position: { x: 160, y: 40 } }),
    new Boundary({ position: { x: 200, y: 40 } }),
];

export default boundaries;
